spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: embarkx
    password: embarkx

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8083
