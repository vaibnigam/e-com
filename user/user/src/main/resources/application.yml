# Minimal local configuration
# Most configurations are managed by config-server
# Only override here if needed for local development or environment-specific settings

# Note: Service name and config server connection are in bootstrap.yml
# MongoDB and server configurations are loaded from config-server
